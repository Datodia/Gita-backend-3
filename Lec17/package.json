{
  "name": "lec17",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "prebuild": "rimraf ./dist",
    "build": "npx tsc",
    "start": "node ./dist/main.js",
    "predev": "npm run build",
    "dev": "concurrently \"tsc -w\" \"node --watch ./dist/main.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "rimraf": "^6.1.3"
  }
}
